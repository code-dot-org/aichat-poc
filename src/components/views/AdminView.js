import React from "react";

// an admin view would be similar to the teacher view, but allow choosing of teachers -> classrooms -> Students
// and presumably be able to view some sort of aggregate statistics or see information across all rooms about inappropriate
// information.
//
// maybe everyone in a school? Or everyone across the entire org?

const AdminView = () => {
  return <div>Admin View</div>;
};

export default AdminView;
